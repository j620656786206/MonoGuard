[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"
watchPaths = ["apps/api/**"]

[deploy]
startCommand = "./main"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "on_failure"

[env]
PORT = "8080"
GIN_MODE = "release"
APP_ENV = "production"